<% @following = Follow.find_by(f_id: @user_name.id, user_id: current_user.id) %>
<% if !@following %>
<%= link_to "", "/#{params[:userid]}/follow", class: "follow_btn follow", remote: true %>
<% else %>
<%= link_to "", "/#{params[:userid]}/unfollow", class: "follow_btn unfollow", remote: true %>
<% end %>